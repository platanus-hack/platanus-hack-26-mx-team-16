# Where the FastAPI backend lives. Used by the BFF (server-side only).
BACKEND_API_HOST=http://localhost:8200

# Public URL of this docs site.
PUBLIC_SITE_URL=http://localhost:4321

# Port the dev server / container listens on.
DOCS_PORT=4321
