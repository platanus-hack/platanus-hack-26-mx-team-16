{
  "slug": "standard-extraction",
  "name": "Standard Extraction",
  "kind": "EXTRACTION",
  "version": 1,
  "output_schema": null,
  "phases": [
    { "id": "ingest", "kind": "ingest" },
    { "id": "extract_text", "kind": "extract_text" },
    { "id": "classify_pages", "kind": "classify_pages" },
    { "id": "extract_fields", "kind": "extract_fields" },
    { "id": "validate_extraction", "kind": "validate_extraction" },
    { "id": "finalize", "kind": "finalize" }
  ]
}
