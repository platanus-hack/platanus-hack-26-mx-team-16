"""Concrete provider adapters (F12 scaffold).

Stub implementations for the OAuth/credential-based providers. Every method
raises :class:`NotImplementedError` until the external OAuth/credential flow is
wired; the classes stay importable so the registry can map providers to them.
"""
